#include "generator.h"


std::string generateTangent(int curOrder, int totalOrder, std::vector<Param> parameters) {
    std::string result = "";

    // If curOrder == totalOrder, seed x (top layer)

    // If curOrder == 1, extract y (bottom layer)

    // If the name ends in _{curOrder}, then we need to seed/extract it

    return result;
}