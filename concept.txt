first compile and run 
    -> generate the AD driver via the configs
        (TODO: the user should define the sequence in the configs)
    -> generates the main function for the second compile and run


second compile and run
    -> run and validate the AD drivers against the formula driver
    this is done by calling runADDrivers, runFormulaDriver, and the validator
    Note: the formulaDriver depends on the existence of ADDrivers to work